module.exports = function multiply(first, second) {
  // your solution
  const makeString = ''
  //number for multiplying
  const firstNumber=BigInt(first)
  const secondNumber=BigInt(second)
  //i wrote all in 1 return but it erroring =/
  return makeString+firstNumber*secondNumber;
}
